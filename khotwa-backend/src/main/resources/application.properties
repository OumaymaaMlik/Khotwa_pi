# Server Configuration
server.port=8084
server.servlet.context-path=/khotwa

# Spring Main
spring.main.allow-bean-definition-overriding=true

# Data & Pageable
spring.data.web.pageable.serialization-mode=via-dto

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/khotwa_db
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Servlet & File Uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hazemnaceur325@gmail.com
spring.mail.password=mytt erit tkaw zncm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Custom App Properties
app.upload-dir=uploads/documents/
app.cors.allowed-origin=http://localhost:4200

app.jwt.expiration-ms=86400000
# Ensure you set an environment variable for JWT_SECRET