package tn.khotwa.biblio.enums;

// FIX: remplace AccessLevel (PUBLIC/INCUBES/PAYANT) par PlanType
// pour correspondre à la structure User
public enum PlanType {
    FREE,
    PREMIUM,
    INSTITUTIONAL
}
